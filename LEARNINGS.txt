you can't put padding at the top or the bottom of inline elements 
--> workaround: display: inline-block;

erst HTML, dann Klassen einfügen und CSS
cursor: pointer;

button sind keine block-level elemente, deshalb margin 0 auto nicht hilfreich --> display: block ist die Lösung

adding last child

inline-block-elements: haben padding, width etc. welche aufaddiert werden --> Puffer schaffen, width nicht auf 100% addieren
flex-elements: haben kein padding etc. deshalb kann man sie auf width 100% aufaddieren

